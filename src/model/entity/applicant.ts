export interface ResumeLicenseRequestInfo {
    username: string;
    address: string;
    status: number;
}

export interface ResumeVisitHistoryInfo {
    ReUserName: string;
    downloadTime: string;
}
